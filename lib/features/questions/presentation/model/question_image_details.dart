class QuestionImageDetails {
  final double logicalScale;
  final double dragVerticalPosition;
  final bool showZoom;
  final List<int> pointerIds;
final double opacity;
  QuestionImageDetails({
    required this.pointerIds,
    required this.logicalScale,
    required this.dragVerticalPosition,
    required this.showZoom,
      required this.opacity
  });
}
